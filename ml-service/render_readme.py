"""
render.yaml — Declaración de los 3 servicios de Render para ml-service.

Render lee este archivo automáticamente si está en la raíz del repositorio
(o en la carpeta que configuras como raíz del servicio).
Ver: https://render.com/docs/blueprint-spec
"""
