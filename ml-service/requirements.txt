# ============================================================
# requirements.txt — ml-service
# Nota: fastapi, uvicorn, sqlalchemy, pydantic, pandas, numpy,
#       scikit-learn, requests, httpx ya están en el entorno
#       anaconda3 del sistema. Solo se listan las dependencias
#       adicionales que hay que instalar.
# ============================================================

# Configuración con .env
pydantic-settings>=2.5.0

# Datos de mercado
yfinance>=0.2.51

# Driver async de PostgreSQL
asyncpg>=0.30.0

# Seguridad (JWT interno)
python-jose[cryptography]>=3.3.0

# Migraciones de BD
alembic>=1.13.0

# Relativedelta para fechas en train.py
python-dateutil>=2.9.0
